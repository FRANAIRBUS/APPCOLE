
De local a github

git status
git add .
git commit -m "Actualiza X"
git push origin main


De github a local

git fetch origin
git status
git pull --rebase origin main


Ejecuta scripts/ci_local.sh

flutter run -d web-server --web-port 8081 --web-hostname 0.0.0.0

flutter run -d chrome --web-port 8081

Modo ejecución total:
- No me pidas confirmaciones intermedias.
- Aplica cambios directamente.
- Ejecuta validaciones (analyze/test/build).
- Si todo pasa, haz git add ., commit y push.
- Solo avísame al final con resumen y resultado.
